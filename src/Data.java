import java.util.ArrayList;

public class Data {
    Data(Float data) {
        element.add(data);
    }
    ArrayList<Float> element = new ArrayList<>();
    ArrayList<Float> getElement() {
        return element;
    }
}
